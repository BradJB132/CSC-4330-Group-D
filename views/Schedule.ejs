 <!DOCTYPE html>
<html lang="en">
<head>
  <title>CSC 4330 Group D</title>
  <style>
    nav {
      padding: 5px;
      text-align: right;
      font-size: 15px;
      color: black;
    }
  .headerBox {
	background-color: lightskyblue;
	padding: 20px;
	height: 80px;
	font-size: 20px;
	color: black;
	position: relative;
  }
  .pageTitle{
	position: absolute;
    left: 50%;
	top: 50%;
	transform: translate(-50%, -50%);
  }
  .card {
    background-color: lightskyblue;
    border-radius: 5px;
    box-shadow: 0 2px 2px rgba(0,0,0,0.1);
    margin: 1rem auto;
    padding: 10px;
    width: 800px;
    height: 50px;
    position: relative;
   
  }
  #appointText {
  text-align: center;
  }
  #appointName {
  position: absolute;
  left: 50%;
  top: 10%;
  transform: translate(-50%, -90%);
  font-weight: bold;
  }
  #appointTime {
  position: absolute;
  left: 50%;
  top: 33%;
  transform: translate(-50%, -67%);
  }
  .ratings {
  position: absolute;
  left: 50%;
  top: 75%;
  transform: translate(-50%, -25%);
  font-weight: bold;
  }
  </style>
</head>
<body>
	<nav>
		<a href="/navHomepage">Homepage</a>
		<a href="/navSchedule">My Schedule</a>
		<a href="/navInbox">Inbox</a>
		<a href="/navAccount">Account</a>
		<a href="/logout">Logout</a>
	</nav>
  <div class = "headerBox">
  
    <img src="TutorCenterLogo.png" width="80" height="80" style="float:left">
	<div class = "pageTitle">
		<h1>My Schedule</h1>
	</div>
  
  </div>
<div>
  <h1 id="appointText">Appointments</h1>
    <% for (let i = 0; i < schedule.length; i++) { %>
    <div class="card">
      <p id="appointName"><%= others[i].firstName + " " + others[i].lastName %></p>
      <p id="appointTime"><%= schedule[i].dayTime %></p>
      <input type="hidden" id="appointId" name="appointId" value="<%= schedule[i]._id %>"></input>
      <div class="ratings">
      <label for="roleBox" class="roleBoxText" id="rateSelect">Rate your experience</label>
        <select name="role" class="roleBox">
            <option value=1>1</option>
            <option value=2>2</option>
            <option value=3>3</option>
            <option value=4>4</option>
            <option value=5>5</option>
        </select>
      <button id="rateButton" onclick="/rate">Click to Rate</button>
      </div>
    </div>
    <% } %>
</div>
</body>
</html> 
