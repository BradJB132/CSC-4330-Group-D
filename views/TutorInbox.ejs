 <!DOCTYPE html>
<html lang="en">
<head>
  <title>CSC 4330 Group D</title>
  <style>
     nav {
      padding: 5px;
      text-align: right;
      font-size: 15px;
      color: black;
    }
  .headerBox {
	background-color: lightskyblue;
	padding: 20px;
	height: 80px;
	font-size: 20px;
	color: black;
	position: relative;
  }
  .pageTitle{
	position: absolute;
    left: 50%;
	top: 50%;
	transform: translate(-50%, -50%);
  }
  .card {
    background-color: lightskyblue;
    border-radius: 5px;
    box-shadow: 0 2px 2px rgba(0,0,0,0.1);
    margin: auto;
    margin-bottom: 1rem;
    padding: 10px;
    width: 800px;
    height: 350px;
    position: relative;
  }
  #accept {
  position: absolute;
  left: 40%;
  top: 40%;
  transform: translate(-60%, -60%);
  }
  #deny {
  position: absolute;
  left: 60%;
  top: 40%;
  transform: translate(-40%, -60%);
  }
  #responseText {
  font-weight: bold;
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  }
  #response {
  position: absolute;
  left: 50%;
  top: 68%;
  transform: translate(-50%, -32%);
  resize: none;
  width: 450px;
  height: 150px;
  }
  </style>
</head>
<body>
    <nav>
        <a href="/navSchedule">My Schedule</a>
        <a href="/navInbox">Inbox</a>
        <a href="/navAccount">Account</a>
	<a href="/logout">Logout</a>
    </nav>
  <div class = "headerBox">
  
    <img src="TutorCenterLogo.png" width="80" height="80" style="float:left">
	<div class = "pageTitle">
		<h1>Tutor Inbox</h1>
	</div>
  
  </div>
  <br>
<div>
<% for (var i = 0; i < messages.length; i++) { %>
    <div class="card">
      <h2><%= students[i].firstName %>  <%= students[i].lastName %></h2>
      <h2><%= messages[i].dayTime %></h2>

      <form action="/accept" method="post">
       <button type="submit" id="accept">Accept Appointment</button>
       <input type="hidden" id="studentId" name="studentId" value="<%= messages[i]._id %>"></input>

      </form>
      <form action="/deny" method="post">
       <button type="submit" id="deny">Deny Appointment</button>
       <input type="hidden" id="messageId" name="messageId" value="<%= messages[i]._id %>"></input>
           <div class="form-control">
              <label for="response" id="responseText">Reason for denial</label>
              <textarea
                name="response"
                id="response"
                cols="40"
                rows="30"
                required
              ></textarea>
            </div>
      </form>
    </div>
  <% } %>
</div>

</body>
</html> 
